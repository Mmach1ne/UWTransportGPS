{
  "name": "@transport/device-sim",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "python simulator.py",
    "setup": "pip install -r requirements.txt",
    "test": "python -m pytest tests/"
  }
}
